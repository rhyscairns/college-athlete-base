# AWS Development Environment Configuration
# This file shows the structure for connecting to AWS dev resources
# Copy this to .env.dev and fill in actual values from AWS

# To get these values, run: ./scripts/get-dev-credentials.sh

# Environment
NODE_ENV=development
ENVIRONMENT=development

# Database (AWS RDS - Development)
# Get endpoint from: aws cloudformation describe-stacks --stack-name DevStack
# Get password from: aws secretsmanager get-secret-value --secret-id development/college-athlete-base/db-credentials
DATABASE_HOST=dev-db.xxxxx.us-east-1.rds.amazonaws.com
DATABASE_PORT=5432
DATABASE_NAME=college_athlete_base
DATABASE_USER=postgres
DATABASE_PASSWORD=<get-from-secrets-manager>
DATABASE_SSL=true

# Connection Pool Configuration (Performance Optimization)
DATABASE_MAX_CONNECTIONS=20        # Maximum pool size (default: 20)
DATABASE_MIN_CONNECTIONS=2         # Minimum pool size (default: 2)
DATABASE_IDLE_TIMEOUT=30000        # Idle timeout in ms (default: 30000)
DATABASE_CONNECTION_TIMEOUT=2000   # Connection timeout in ms (default: 2000)
DATABASE_STATEMENT_TIMEOUT=10000   # Query timeout in ms (default: 10000)

# Alternative: Use full DATABASE_URL instead of individual components
# DATABASE_URL=postgresql://postgres:<password>@dev-db.xxxxx.us-east-1.rds.amazonaws.com:5432/college_athlete_base?sslmode=require

# Redis (AWS ElastiCache - Development)
# Get endpoint from: aws cloudformation describe-stacks --stack-name DevStack
REDIS_URL=redis://dev-cache.xxxxx.cache.amazonaws.com:6379

# Authentication
BCRYPT_ROUNDS=10

# JWT Configuration
# Generate a secure random string for JWT_SECRET (e.g., openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=7d

# CORS - Allow localhost and dev domain
ALLOWED_ORIGINS=http://localhost:3000,https://dev.collegeathletebase.com

# Application Settings
LOG_LEVEL=debug
PORT=3000

# Next.js
NEXT_PUBLIC_API_URL=http://localhost:3000

# Notes:
# 1. Never commit .env.dev to Git (it's in .gitignore)
# 2. You need network access to AWS resources (VPN, SSH tunnel, or security group rule)
# 3. See docs/AWS_DEV_ENVIRONMENT_ACCESS.md for detailed setup instructions
# 4. If using SSH tunnel, change DATABASE_HOST to localhost and DATABASE_PORT to your tunnel port
