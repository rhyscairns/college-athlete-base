# Local Development Environment Configuration
# Copy this file to .env.local and update values as needed

# ============================================
# DATABASE CONFIGURATION (Local Docker)
# ============================================
# Use these settings when running PostgreSQL via docker-compose
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=college_athlete_base
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_SSL=false

# Connection Pool Configuration (Performance Optimization)
DATABASE_MAX_CONNECTIONS=20        # Maximum pool size (default: 20)
DATABASE_MIN_CONNECTIONS=2         # Minimum pool size (default: 2)
DATABASE_IDLE_TIMEOUT=30000        # Idle timeout in ms (default: 30000)
DATABASE_CONNECTION_TIMEOUT=2000   # Connection timeout in ms (default: 2000)
DATABASE_STATEMENT_TIMEOUT=10000   # Query timeout in ms (default: 10000)

# Alternative: Use full connection string instead of individual variables
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/college_athlete_base

# ============================================
# AUTHENTICATION & SECURITY
# ============================================
# Password hashing rounds (10 is recommended for development, 12+ for production)
BCRYPT_ROUNDS=10

# JWT Configuration
# IMPORTANT: Generate a secure random string for JWT_SECRET
# Methods to generate:
#   - Node.js: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#   - OpenSSL: openssl rand -hex 64
#   - Python: python3 -c "import secrets; print(secrets.token_hex(64))"
# NEVER commit your actual JWT_SECRET to version control!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=7d

# ============================================
# CORS CONFIGURATION
# ============================================
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000

# ============================================
# APPLICATION SETTINGS
# ============================================
NODE_ENV=development
LOG_LEVEL=debug

# ============================================
# REDIS CONFIGURATION (Optional - for future use)
# ============================================
REDIS_URL=redis://localhost:6379

# ============================================
# AWS DEV ENVIRONMENT (Alternative Configuration)
# ============================================
# To connect to AWS dev database instead of local Docker:
# 1. Run: npm run dev:setup
# 2. Uncomment and update these values:
# DATABASE_HOST=dev-db.xxxxx.us-east-1.rds.amazonaws.com
# DATABASE_PORT=5432
# DATABASE_NAME=college_athlete_base
# DATABASE_USER=postgres
# DATABASE_PASSWORD=<from-secrets-manager>
# DATABASE_SSL=true
# ALLOWED_ORIGINS=http://localhost:3000,https://collegeathletebase-dev.com
